package com.broadcastsim.core.scenario;

/** Identifies the scenario changes supported by the minimum viable simulation. */
public enum ScenarioEventType {
  SET_PROPERTY,
  DEVICE_ENABLE,
  DEVICE_DISABLE
}
