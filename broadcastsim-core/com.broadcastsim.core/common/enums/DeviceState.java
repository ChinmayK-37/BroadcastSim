package com.broadcastsim.core.common.enums;

/** Represents the lifecycle state of a simulated broadcast device. */
public enum DeviceState {
  CREATED,
  ONLINE,
  WARNING,
  FAILED,
  RECOVERING,
  OFFLINE
}
